# disaster_response_docker_pipeline
Automated, Dockerized ETL pipeline for ingesting, transforming, and loading FEMA Disaster Data into a PostgreSQL database. Ensures data readiness for downstream analytics and real-time visualization (e.g., Power BI) through Continuous Integration (GitHub Actions).
